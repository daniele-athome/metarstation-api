{
  "name": "weather-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --env production-gallicano",
    "dev": "wrangler dev --env local",
    "start": "wrangler dev --env local",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.7",
    "@cloudflare/workers-types": "^4.20260127.0",
    "vitest": "~3.2.0",
    "wrangler": "^4.60.0"
  },
  "dependencies": {
    "itty-router": "^5.0.22"
  }
}
